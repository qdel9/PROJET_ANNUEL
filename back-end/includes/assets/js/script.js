// Fonction pour trier les logs
function sortLogs(sortBy) {
    // Envoyer une requête AJAX pour récupérer les logs triés
    // Mettre à jour la page avec les logs triés
}

// Fonction pour éditer un log
function editLog(logId) {
    // Envoyer une requête AJAX pour récupérer les données du log avec l'ID donné
    // Afficher un formulaire pré-rempli avec les données du log
    // Lorsque l'utilisateur soumet le formulaire, envoyer une requête AJAX pour mettre à jour le log dans la base de données
}

// Fonction pour supprimer un log
function deleteLog(logId) {
    // Demander une confirmation à l'utilisateur
    if (confirm("Êtes-vous sûr de vouloir supprimer ce log ?")) {
        // Envoyer une requête AJAX pour supprimer le log avec l'ID donné de la base de données
        // Mettre à jour la page pour refléter la suppression du log
    }
}
